package com.module.DesignMode.observer.pull;

import java.util.Observable;
import java.util.Observer;

public class WeatherData implements Observer {
    @Override
    public void update(Observable o, Object arg) {

    }
}
